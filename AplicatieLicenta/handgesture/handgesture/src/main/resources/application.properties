# Database configuration
spring.datasource.url = jdbc:mysql://localhost:3306/licenta
spring.datasource.username = root
spring.datasource.password = 16092001

# Hibernate dialect configuration
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Flyway configuration for database migrations
spring.flyway.baseline-on-migrate=true

# Open JPA session for view rendering
spring.jpa.open-in-view = true

spring.thymeleaf.prefix=classpath:/templates/

# The days until the deletion of the user
handgesture.daysUntilDeletion=7

handgesture.daysForEmailConfirmation=7

# The secret key for JWT Token
handgesture.secretKey=handGesturehandGesturehandGesturehandGesturehandGesturehandGesturehandGesture

spring.mail.username=testemailsender555@gmail.com
spring.mail.password=ajcm ymxa rgyk yhra
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.freemarker.templateLoaderPath=classpath:/email-templates/
